import styled from "styled-components";

export const ExtraIndexContainer = styled.div`
  // background: ${(props) => props.theme.noteColor};
  // position: absolute;
  // display: flex;
  // align-item: center;
  height: 40px;
  width: auto;
  rotate: -10deg;
  cursor: grab;
  border: 1px solid red;
  border-radius: 10px;
  padding: 5px;
`;
